<network-security-config>
    <!-- 에뮬레이터에서 PC 로컬 서버 접속 -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
    <!-- 실기기에서 PC로 접속한다면 PC IP를 추가 -->
    <!--
    <domain-config cleartextTrafficPermitted="true">
      <domain includeSubdomains="true">192.168.x.x</domain>
    </domain-config>
    -->
</network-security-config>