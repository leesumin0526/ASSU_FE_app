<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.card.MaterialCardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@android:color/transparent"
        app:cardCornerRadius="8dp"
        app:cardElevation="3dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <!-- 배경 이미지 -->

            <!-- 기본 문구 (학교를 선택해주세요) -->
            <ImageView
                android:id="@+id/bg_school_dropdown"
                android:layout_width="400dp"
                android:layout_height="105dp"
                android:src="@drawable/bg_school_drop_down"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/tv_school_default"
                android:layout_width="345dp"
                android:layout_height="wrap_content"
                android:text="학교를 선택해주세요"
                android:textSize="14sp"
                android:fontFamily="@font/pretendard_medium"
                android:textColor="@color/assu_font_sub"
                android:gravity="start|center_vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginTop="20dp" />

            <!-- 구분선 -->
            <ImageView
                android:id="@+id/school_divider"
                android:layout_width="313dp"
                android:layout_height="1dp"
                android:layout_marginTop="12dp"
                android:layout_marginStart="16dp"
                android:background="@color/assu_box"
                app:layout_constraintTop_toBottomOf="@id/tv_school_default"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- 학교 리스트 항목 -->
            <TextView
                android:id="@+id/tv_school_1"
                android:layout_width="345dp"
                android:layout_height="wrap_content"
                android:text="숭실대학교"
                android:textSize="14sp"
                android:fontFamily="@font/pretendard_medium"
                android:textColor="@color/assu_font_sub"
                android:gravity="start|center_vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                app:layout_constraintTop_toBottomOf="@id/school_divider"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="12dp" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
